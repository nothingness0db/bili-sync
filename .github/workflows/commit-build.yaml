name: Build Main Binary

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-binary:
    if: ${{ !startsWith(github.ref, 'refs/tags/') }}
    uses: nothingness0db/bili-sync/.github/workflows/build-binary.yaml@main
